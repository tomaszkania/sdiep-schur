# Notebooks

This directory is reserved for optional exploratory notebooks. The reference, reproducible pipeline is the script

```bash
python scripts/reproduce_paper.py
```

which writes CSV outputs to `data/` and figure PDFs to `paper/figures/`.
